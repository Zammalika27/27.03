#include <stdio.h>
int main()
{
    int a, b, c;
    printf("Введите три числа: ");
    scanf("%d %d %d", &a, &b, &c);
    int mult = a*b*c;
    int sign = (mult>0)-(mult<0);
    printf("%d\n", sign);
    return 0;
}
